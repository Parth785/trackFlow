server:
  port: 8083

spring:
  application:
    name: agent-service

  data:
    mongodb:
      host: localhost
      port: 27017
      database: trackflow

    redis:
      host: localhost
      port: 6379